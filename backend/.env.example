# Comma-separated CORS origins (default: http://localhost:5173,http://localhost:4173)
RECIPE_CORS_ORIGINS=

# Phase 3 — AI caption structuring
ANTHROPIC_API_KEY=

# Phase 2 — Supabase (server-only; service role bypasses RLS, never expose)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=
