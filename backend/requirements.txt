# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30

# Recipe page fetching + parsing
httpx>=0.27
beautifulsoup4>=4.12

# AI structuring (phase 3: caption/paste extraction)
anthropic>=0.52

# Image mirroring (phase 7: resize a saved photo before it goes in the bucket)
pillow>=10.4

# Supabase is reached over plain HTTP with the httpx above, not through a client
# library: auth verifies by asking GoTrue (app/auth.py), usage and storage are
# two REST calls. pyjwt and supabase were both installed and never imported —
# cold-start weight on a function that already carries Pillow.
